# Releases

Published appliance bundles live in this directory.

Current artifact:

- `slime-appliance-v0.2.0.tar.gz`

Next planned candidate artifact:

- `slime-appliance-v0.2.1-hello-world-candidate.tar.gz`

Planned manifest:

- `HELLO_WORLD_CANDIDATE_MANIFEST.txt`

Integrity:

- `SHA256SUMS` contains the published SHA-256 digest for the bundle

The release bundle is a packaging surface.
It does not replace the public `SLIME` canon.
